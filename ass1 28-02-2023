//c program to print count of prime numbers between the given range.
#include<stdio.h>
#include<conio.h>
int main()
{
int i,n,j,c;
printf("enter the range");
scanf("%d",&n);
for(i=1;i<=n;i++)
{
c=0;
for(j=1;j<=i;j++)
{
if(i%j==0)
c=c+1;
}
if(c==2)
printf("%d",i);
}
return 0;
}
